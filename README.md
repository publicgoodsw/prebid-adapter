# prebid-adapter

Example setup:

```
{
        bidder: 'publicgood',
        params: {
          partnerId: '{ provided by public good}',
          slotType: 'all | { optional slot identifier }'
}
```
